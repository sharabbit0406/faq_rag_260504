開兩個終端，分別執行：

**終端 1 — 後端**

`cd "C:\Users\sha\Documents\生成式AI學習\claude code\project1\backend"
uv run uvicorn app.main:app --reload --port 8000`

看到 `Application startup complete.` 就代表啟動成功。

後端有 

`uv run uvicorn app.main:app --reload --port 8000` 的話會自動重新載入

後端跑的是舊版本。

請在後端終端按 **Ctrl+C**，然後執行這個指令（先清 pycache 再啟動）：

`cd "C:\Users\sha\Documents\生成式AI學習\claude code\project1\backend"
Get-ChildItem -Recurse -Filter "__pycache__" | Remove-Item -Recurse -Force
uv run uvicorn app.main:app --reload --port 8000`

清掉 pycache 確保不用舊的 `.pyc` 快取檔。

---

**終端 2 — 前端**

`cd "C:\Users\sha\Documents\生成式AI學習\claude code\project1\frontend"
npm run dev`
看到 `Ready in ...ms` 就代表前端跑起來了，然後開瀏覽器進 `http://localhost:3000`。

**3. 啟動資料庫容器**

確認 Docker 啟動後，在終端機執行：
# 在專案根目錄執行
`cd "c:\Users\sha\Documents\生成式AI學習\claude code\project1"

docker compose up -d 

確認 PostgreSQL 正常

docker ps --filter "name=db"


方案 B（治本）： 固定 Hyper-V 的動態埠起點，讓它永遠不碰 5542。


# 以系統管理員身分執行，重開機後生效
netsh int ipv4 set dynamicport tcp start=49152 num=16384
執行後重開機，5542 就不會再被保留。
